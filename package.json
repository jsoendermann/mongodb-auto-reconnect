{
  "name": "mongodb-auto-reconnect",
  "version": "0.0.3",
  "license": "Apache-2.0",
  "main": "./index.js",
  "typings": "./index.d.ts",
  "scripts": {
    "build": "tsc --pretty",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "typescript": "^4.0.2"
  }
}
